# Personal operator runbook + webhook URL — never commit
.env.local
OPERATIONS.md

# The configurator project lives in its own repo:
# https://github.com/revolutionaryPhoton/f13-configurator.git
configurator_v1/

# Ralph loop runtime logs (per-iteration JSON, usage data)
ralph-logs/

# Local agent skills — contain personal paths, never commit
.claude/

# Standalone design-exploration repo, lives next to configurator/ for
# convenience but unrelated to the ralph loop
f13-configurator-design/

# OS noise
.DS_Store
Thumbs.db

# Local-only reference of intended branch-protection settings; UI
# is the source of truth, this file is a private snapshot
docs/BRANCH_PROTECTION.md
.ralph-sbx/
